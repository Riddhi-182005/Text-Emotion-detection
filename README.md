# Text Emotion Detection Project

This is a simple text-based emotion detection project.
It uses a pre-trained transformer model to detect emotions such as joy, sadness, anger, and neutral.
The user can enter any sentence and get the predicted emotion along with the confidence score.
No dataset creation or model training is required; the model is ready to use.
